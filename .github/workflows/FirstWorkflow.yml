name: My First Workflow
on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  first_job:
    name: My first job
    runs-on: ubuntu-latest
    steps:
      - name: Great the user
        run: |
          echo "Hello world from ${{ vars.MY_VARIABLE }}!"
          echo "My secret is branch is ${{ secrets.MY_SECRET }}"
        shell: bash

      - name: Checkout
        uses: actions/checkout@v6.1.0

      - run: tree
